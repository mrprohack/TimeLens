const sidebarHistory = document.getElementById('sidebar-history');
const historyTrigger = document.getElementById('open-history');

sidebarHistory?.addEventListener('click', () => historyTrigger?.click());
